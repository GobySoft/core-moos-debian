--- a/Essentials/pMOOSBridge/CMakeLists.txt
+++ b/Essentials/pMOOSBridge/CMakeLists.txt
@@ -15,3 +15,8 @@
 
 # Link the executable
 TARGET_LINK_LIBRARIES(pMOOSBridge ${MOOSLIBS} ${PLATFORM_LIBS} )
+
+INSTALL(TARGETS
+  pMOOSBridge
+  RUNTIME DESTINATION ${CMAKE_INSTALL_PREFIX}/bin
+  )
--- a/Essentials/pScheduler/CMakeLists.txt
+++ b/Essentials/pScheduler/CMakeLists.txt
@@ -14,3 +14,9 @@
 
 # indicate how to link
 TARGET_LINK_LIBRARIES(pScheduler ${MOOSLIBS} ${PLATFORM_LIBS} )
+
+
+INSTALL(TARGETS
+  pScheduler
+  RUNTIME DESTINATION ${CMAKE_INSTALL_PREFIX}/bin
+  )
--- a/Tools/Graphical/uMS/CMakeLists.txt
+++ b/Tools/Graphical/uMS/CMakeLists.txt
@@ -46,6 +46,14 @@
    # Link the executable
    TARGET_LINK_LIBRARIES( uMS ${MOOSLIBS} ${PLATFORM_LIBS} fltkvw ${FLTK_LIBRARIES} ${OPENGL_LIBRARIES} )
 
+   INSTALL(TARGETS
+     uMS
+     RUNTIME DESTINATION ${CMAKE_INSTALL_PREFIX}/bin
+     )
+
+
+
+
 ELSE(FLTK_FOUND)
     
    MESSAGE("uMS won't be built as FLTK was not found.")
